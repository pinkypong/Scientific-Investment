# AGENTS.md — 항상 적용 규칙 (AI주식리서치)

이 파일의 규칙은 이 프로젝트에서 작업할 때 **항상 우선 적용**한다.

## 1. 산출물 저장 위치 (document-output-location)

사용자의 모든 결과물(문서·리포트·대시보드·스크립트·데이터·정리자료)은
임시 outputs 폴더가 아니라 **아래 문서 보관 폴더**에 저장한다.

실행 환경에 따라 루트가 다르다. **먼저 어디서 돌고 있는지 확인하고** 그 루트를 쓴다.

| 환경 | 루트 |
|---|---|
| 로컬 (Windows / OneDrive) | `C:\Users\eigoo\OneDrive\문서\Claude\Projects\<프로젝트명>` |
| GitHub Codespaces / 컨테이너 | 레포 루트 (`/workspaces/<repo>`) — OneDrive 경로는 존재하지 않는다 |

- 로컬 사용자는 `eigoo` 다. (스킬 기본값 `C:\Users\eigoo\Documents\Claude_Document` 는 이 PC에 없음 → 위 경로 사용.)
- OneDrive 폴더라 **모바일에도 자동 동기화**된다(모바일 열람/이어작업의 다리).
- ⚠️ `...\Claude` 상위 폴더는 내부 `Scheduled`(보호 폴더) 때문에 통째로 마운트 불가.
  → **반드시 `Projects\<프로젝트명>` 같은 하위 폴더**를 만들어(또는 골라) 연결한다.
- Codespaces 에서는 경로를 하드코딩하지 말고 레포 상대경로를 쓴다. 모든 파이썬 명령은
  레포 루트에서 `python -m data_sources.*`.

## 2. 폴더 구조 규칙

작업 단위로 하위 폴더를 만들고 루트에 파일을 흩뿌리지 않는다.
```
Projects\<프로젝트명>\
  └─ <세부주제>\
       ├─ README_핸드오프.md   ← 맥락·데이터·다음 할 일 (이어작업용)
       ├─ <산출물>.html/.md/.docx/.pptx/.xlsx
       ├─ data\                ← json/csv 등 데이터
       └─ 원본자료\             ← 업로드 PDF·캡처·참고자료 원본
```
- 프로젝트/폴더명은 나중에 제목만 봐도 알아볼 수 있게 구체적으로.
- **원본 자료도 함께** 넣어 폴더 하나로 맥락이 복원되게 한다.

## 3. 저장 후

- 반드시 `mcp__cowork__present_files`로 링크를 띄운다(경로만 글로 적지 않기).
- 완성본만 이 폴더로 옮기고, 중간 임시파일은 outputs에서 처리.
- 기존 파일 덮어쓰기 전 사용자 확인. 삭제 권한 없을 수 있음 → 잔여물은 사용자에게 안내.

## 4. 분석 규칙

- 반도체/메모리/AI 종목·리포트 분석은 **semiconductor-ai-investment-research 스킬**(계정 저장, 모바일에서도 작동)의 13블록·검산·확률 프레임워크를 따른다.
- 목표주가·컨센서스는 진실이 아님. 확률은 근거를 밝히고 subjective/frequency를 구분.
