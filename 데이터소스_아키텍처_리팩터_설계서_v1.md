# Financial Data Ingestion Architecture — 리팩터 설계서 v1

> 작성 2026-08-27 · 지시: "Financial Data Source Integration Refactor" (Phase 0 감사 → Phase 1 어댑터 → Phase 2 공통 스키마)
> SSOT 위계상 `프로젝트_감사_및_MasterSpec_v1.md` 아래, `데이터계보_감사_설계서_v1.md`와 병렬. AGENTS.md·플랫폼_설계서 규칙 상시 적용.
> **이번 iteration 범위 = Phase 0–2.** Phase 3–5(incremental/cache/dedup, validation/health, research intelligence)는 후속.

---

## Phase 0 — AUDIT (코드 미수정, 실제 파일 대조)

### 0.1 현재 구조

**Hankyung Global Market:** 연결 코드 **없음**. 저장소 전체 grep 결과 `hankyung` 0건. 이번 세션에서 만들기 시작한 `리포트_파이프라인/collectors/hankyung_global.py`(스켈레톤)만 존재.

**Hankyung Consensus:** 연결 코드 **없음**. 과거 세션에서 Claude가 `반도체_메모리_대시보드/원본자료/*.pdf`(키움·삼성·미래에셋 3종)를 수동 판독 → 추출 숫자가 `CD.fin[k].opt` 산문(prose)에 baked-in. 이번 세션 `리포트_파이프라인/collectors/hankyung_consensus.py`(스켈레톤)만 존재.

**bigdata.com:** 유일한 라이브 소스. **브라우저에서** `window.cowork.callMcpTool(...)`로 호출(데스크톱 Cowork 전용). 호출 지점 2곳 + 분석 1곳:
| 함수 | 위치 | 하는 일 |
|---|---|---|
| `refreshQuotes()` | [index.html:781](반도체_메모리_대시보드/index.html#L781) | `bigdata_company_tearsheet` × 4 (하드코딩 `IDS` rp_entity_id) → `price_performance.current_market.current_price` → 인메모리 `MC`/`CD` 변이, `exp_ret`/`p_up*`/`p_dn*` 인라인 재계산([L791-793](반도체_메모리_대시보드/index.html#L791-L793)), rerender. **영속화 없음.** |
| `loadNews()` | [index.html:803](반도체_메모리_대시보드/index.html#L803) | `bigdata_search` fast × 2 (NEWS/report doc type), D-2, 하드코딩 키워드 쿼리([L808](반도체_메모리_대시보드/index.html#L808)) → 내용 기반 리포트/뉴스 분류 → `lastReports`/`lastNews` 전역 |
| `analyzeReports()` | [index.html:848](반도체_메모리_대시보드/index.html#L848) | `window.cowork.askClaude(prompt, data)` — Haiku 스니펫 분석. Rule 3 준수(가정 자동변경 없음) |

**Database:** **없음.** "DB" 역할 = 대시보드 스크립트 상단 인라인 리터럴 `var MC={...}`([index.html:435](반도체_메모리_대시보드/index.html#L435)) + `var CD={...}`([index.html:436](반도체_메모리_대시보드/index.html#L436)), 4개 종목. `data/mc.json`·`cdata.json`·`samsung_dcf.json`는 **런타임에 로드 안 됨**(fetch 없음) — 핸드오프/재현용 스냅샷 사본. `web_deploy/index.html`은 `index.html`과 **바이트 동일 수동 사본**.

**Update mechanism:** `refreshQuotes` 버튼(데스크톱) → 인메모리만 갱신, 새로고침하면 스냅샷(2026-08-21)으로 복귀. 영속·증분·이력 전무.

**Provenance:** UI는 이미 상당함 — `openAudit()`([L476](반도체_메모리_대시보드/index.html#L476)), `LN(co,key)`([L501](반도체_메모리_대시보드/index.html#L501), 거대 `if(key===...)` 사다리), `linNode`/`auditTree`/`openFullAudit`([L591-622](반도체_메모리_대시보드/index.html#L591-L622), 재귀 트리). 그러나 **전부 JS 리터럴 하드코딩**. Source 문자열(`'Bigdata.com (FMP)'`), 날짜(`'2026-08-19'`)가 코드에 박혀 있고 데이터에서 파생되지 않음.

### 0.2 발견한 문제 (Critical / Important / Optional)

**Critical**
- **C1. 지시 전제가 사실과 다름** — 한경 2소스 미연결, DB 없음. → 스펙의 adapter→normalize→validate→**DB**→analytics→dashboard를 **파일 기반 파이프라인**으로 실현해야 함(사용자 확정).
- **C2. 데이터·provenance가 코드에 하드코딩** — 숫자·출처·날짜·신뢰도가 `MC`/`CD`/`LN()` 리터럴. 새 데이터가 오면 사람이 JS를 고쳐야 함. 재현·역추적이 "코드 읽기"에 의존.
- **C3. Raw preservation·이력 없음** — `refreshQuotes`는 값을 덮어쓰고 버림. 컨센서스 revision(스펙 §5), time-series(스펙 §7), snapshot(스펙 §17) 불가.
- **C4. MCP 서버 UUID 하드코딩** — `mcp__4b6a0cee-...__bigdata_*`가 [L787](반도체_메모리_대시보드/index.html#L787)·[L809](반도체_메모리_대시보드/index.html#L809) 문자열에 박힘. 환경 바뀌면 깨짐.

**Important**
- **I1. source-specific 로직 산재** — 4-슬러그 병렬 딕트(`ORDER`/`SECT`/`CONF`/`VCONF`/`COV`/`IDS`/`tpsrc`) ~15곳 반복. 종목 추가 = 다지점 수정.
- **I2. 파생·원본 미분리** — `exp_ret`/`p_up*`가 `refreshQuotes` 안에서 즉석 계산([L791-793](반도체_메모리_대시보드/index.html#L791-L793)), formula/입력 기록 없음(스펙 §9).
- **I3. `MC` vs `CD.fin` 중복 필드** — `cur`/`ccy`/`name`/`tp` 양쪽 보유, 두 곳에서 갱신.
- **I4. 검증·health·retry·dedup·cache 전무** — `refreshQuotes`는 per-ticker `catch(e){console.error}` 후 "ok/4"만.
- **I5. `web_deploy` 수동 이중화** — 편집마다 복사 필요.
- **I6. 뉴스 쿼리 하드코딩** — [L808](반도체_메모리_대시보드/index.html#L808) 키워드 문자열이 커버 종목에서 파생되지 않음.

**Optional**
- O1. 날짜 리터럴 산재(`2026-08-19/13/21`). O2. Home vs Memory>개요 요약 중복(MasterSpec §14-1). O3. `data/*.json` 스냅샷이 런타임과 분리돼 drift 가능.

### 0.3 재사용할 코드 (스펙: "정상 작동 코드 재작성 금지")

| 스펙 요구 | 이미 있는 것 | 위치 |
|---|---|---|
| Number Types / Source Classification 배지 | `badge()` + `.lbc fact/cons/model/assum/scen/ins` + `.tier a/b/c` | [index.html:475](반도체_메모리_대시보드/index.html#L475), CSS [L18-24](반도체_메모리_대시보드/index.html#L18-L24) |
| Provenance popup / dependency tree (스펙 §8) | `linNode()` `auditTree()` `openFullAudit()` (재귀 drill-down) | [index.html:591-622](반도체_메모리_대시보드/index.html#L591-L622) |
| Common Data Interface (스펙 §2) 골격 | `LN()` 노드 스키마 `{label,value,type,confidence,formula,calc,source,sourceDate,verification,why[],evidence[],contra[],parents[],missing[],method}` | [index.html:501-588](반도체_메모리_대시보드/index.html#L501-L588) |
| MC 확률 수학 | `pAbove`/`pBelow` | [index.html:777-778](반도체_메모리_대시보드/index.html#L777-L778) |
| MCP 호출 패턴 | `window.cowork.callMcpTool` in `refreshQuotes`/`loadNews` | [index.html:787](반도체_메모리_대시보드/index.html#L787), [809](반도체_메모리_대시보드/index.html#L809) |
| 파일 기반 파이프라인 관행 | `학습자료/_damodaran_pipeline/build.py` (raw txt → 커밋된 JSON) | — |
| 프로토타입 게이트 | `학습자료/prototypes/*.html` + AGENTS.md §3 + memory `prototype-before-save` | — |
| 이번 세션 한경 스켈레톤 | `리포트_파이프라인/collectors/{hankyung_consensus,hankyung_global}.py`, `common.py` | → `data_sources/`로 흡수 |
| 섹터 벤치마크 | `학습자료/damodaran_allsectors.json` (54섹터) | — |

### 0.4 Migration 필요 여부 — **예 (제한적, 비파괴)**

`MC`/`CD`는 렌더 레이어가 직접 읽는 **계약(contract)**. 정규화 스토어로 바로 대체하면 대시보드가 깨짐. →
- 스토어를 **추가**하고, 빌드 단계가 스토어에서 `MC`/`CD`(+ 신규 `DS` provenance 객체)를 **재생성**해 인라인 주입.
- `LN()`은 함수별로 `DS` 조회 우선 + 리터럴 fallback으로 점진 교체. 스토어가 비면 동작 100% 동일.
- 일회성 `migrate_inline_to_store.py`: 현재 `MC`/`CD` + `LN()` 리터럴 → 스토어에 **역사적 스냅샷**으로 시드(출처 아는 건 그 source_type, 모르면 `UNVERIFIED`).

---

## Phase 1–2 — 목표 아키텍처 (파일 기반)

```
AI주식리서치/
  data_sources/
    common/
      schema.py          # NormalizedRecord · ProvenanceRecord · DerivedMetric (dataclass + JSON ser)
      classification.py  # SourceClass / NumberType enum + provider→class 매핑 (config override 가능)
      normalization.py   # 통화·기간(FY/Q) 파싱, metric 명 정규화, dedup 키 생성
      validation.py      # validate(record) → VALID|WARNING|ERROR + 사유 (null/NaN/inf·음수매출·급변·통화불일치·기간불일치)
      provenance.py      # value → record → document 체인 구성/추적
      retry.py           # retry_with_backoff(fn, retries=3, base=1s) + timeout
      cache.py           # 파일 TTL 캐시 (market=short · reports=long · static=very long)
      store.py           # append-only JSONL 스냅샷 스토어 + sync_state + source_health + 로깅
      provider.py        # ABC: MarketDataProvider · ResearchReportProvider · NewsProvider · FundamentalProvider
    bigdata/       {adapter.py, parser.py, schema.py, README.md}   # MCP tearsheet/search 계약 래핑, 서버 id=config
    hankyung_consensus/  {adapter.py, parser.py, schema.py, README.md}  # 리포트_파이프라인/collectors/* 이전·리팩터
    hankyung_global/     {adapter.py, parser.py, schema.py, README.md}  # 〃 (Playwright 예상실적)
    config/
      data_sources.json  # providers·classification override·update policy·cache TTL·MCP 서버 id·세션 경로
    store/               # "DB" (생성물, append-only)
      <source>/*.jsonl · sync_state.json · source_health.json · raw/<source>/...
    build_dashboard_data.py   # store → normalize → derive → dashboard_data.json + index.html/web_deploy 인라인 주입(MC·CD·DS·SRC·HEALTH)
    run_sync.py               # update policy에 따라 provider별 증분 fetch → validate → store → health (Phase 1은 수동 실행)
    migrate_inline_to_store.py  # 일회성: 현재 MC/CD/LN() → 스토어 시드
  데이터소스_아키텍처_리팩터_설계서_v1.md   # 이 문서
```

### Common Data Interface (스펙 §2) — `common/schema.py`
`NormalizedRecord` 필수: `source, provider, original_url, retrieved_at, as_of_date`. 선택: `source_type, published_at, updated_at, ticker, company_name, market, currency, metric, value, unit, period, document_type, report_title, analyst, broker, is_derived, formula, input_record_ids, confidence, validation_status, raw_ref`. → 기존 `LN()` 노드로 **손실 없이 사상**(mapping) 가능해야 함.

### Source Classification (스펙 §3) — `common/classification.py`
`PRIMARY_OFFICIAL · SECONDARY_PROFESSIONAL · MARKET_DATA · NEWS · DERIVED · AI_GENERATED`.
기본 매핑: 한경 컨센서스→`SECONDARY_PROFESSIONAL` · 한경 글로벌마켓→`NEWS`(수치 섹션은 `SECONDARY_PROFESSIONAL`) · bigdata.com→`MARKET_DATA`. 향후 SEC/FRED/OpenDART→`PRIMARY_OFFICIAL` 자리 마련. UI 배지는 기존 `.lbc`/`.tier`에 매핑.

### Provider 인터페이스 (스펙 §21) — `common/provider.py`
```python
class ResearchReportProvider(ABC):
    def list_reports(self, ticker, since, until, limit): ...
    def fetch_report(self, report_id): ...        # PDF/원문
    def normalize(self, raw) -> list[NormalizedRecord]: ...
```
구현체: `HankyungConsensusProvider`, `HankyungGlobalProvider`, `BigdataProvider`(Market+News). business logic은 인터페이스만 호출.

### Provenance & Derived (스펙 §8·§9) — `common/provenance.py`
- `store/`의 모든 레코드는 불변·append-only. 갱신 = 새 스냅샷 행 추가(스펙 §5·§17).
- 파생값은 `is_derived=true` + `formula` + `input_record_ids` + `calculated_at`. 계산 결과만 저장해 원본 소실 금지.
- `build_dashboard_data.py`가 `DS[co][key]` = 최신 유효 레코드 + 부모 체인 → 대시보드 `LN()`이 조회.

### Validation (스펙 §16) — `common/validation.py`
자동 삭제 금지. `validation_status ∈ {VALID, WARNING, ERROR}` + 사유. 이상치도 스토어에 남기고 플래그(스펙 §16). 기존 "DATA CONFLICT 차단"(검증시스템_설명.md) 로직을 이 레이어로 흡수.

### 대시보드 변경 (Phase 2, 최소·비파괴)
1. 인라인 블록 `/* DS-DATA-START */ var DS={}; var SRC={mcp_server:'',ids:{}}; var HEALTH=[]; /* DS-DATA-END */` 추가 — `build_dashboard_data.py`가 채움.
2. `LN(co,key)` 첫 줄: `var r=DS&&DS[co]&&DS[co][key]; if(r)return r;` — 스토어 비면 기존 리터럴 그대로(무변경).
3. `refreshQuotes`/`loadNews`의 MCP 서버 UUID·`IDS` → `SRC.mcp_server`/`SRC.ids` 참조로 치환(C4 해소).
4. Governance 뷰에 "Source Health" 패널(`HEALTH` 렌더).
5. `build_dashboard_data.py`가 `index.html`+`web_deploy/index.html` 동시 기록(I5 해소).

### 기존 기능 보호 (스펙 §26) — 각 단계 후 확인
`node --check` (index.html `<script>` 추출) · 브라우저로 Home/Screener/Memory/Governance 로드 · 4종 탭 렌더 · Audit 팝업(숫자 클릭)·Full Audit 트리 · `refreshQuotes`/`loadNews` 동작(데스크톱) · `data/*.json` 스냅샷 보존.

---

## 구현 순서 (Phase 0–2)

1. **[P0] 이 문서 = 감사 보고** (완료).
2. **[P1] `data_sources/common/` 스캐폴드** — schema·classification·provider·normalization·validation·retry·cache·store (동작하는 최소 구현).
3. **[P1] 어댑터 3종** — `리포트_파이프라인/collectors/*` → `data_sources/{hankyung_consensus,hankyung_global}/` 이전·리팩터 + `data_sources/bigdata/` 신설(MCP 계약 래핑). 각 `Provider` 구현체 + `normalize()`.
4. **[P1] `config/data_sources.json`** — 하드코딩(서버 id·IDS·업데이트 정책·TTL·세션 경로) 외부화.
5. **[P2] `store.py` + `migrate_inline_to_store.py`** — 현재 `MC`/`CD`/`LN()` → append-only 스토어 시드.
6. **[P2] `build_dashboard_data.py`** — 스토어 → `MC`/`CD`/`DS`/`SRC`/`HEALTH` 재생성·인라인 주입(`index.html`+`web_deploy`). 빈 스토어 시 대시보드 무변경 검증.
7. **[P2] 대시보드 `DS` 훅** — `LN()` 조회 우선 + fallback, `SRC` 참조, Governance Source Health 패널.
8. **검증** — 위 "기존 기능 보호" 체크리스트 전부 + `python -m data_sources.run_sync --dry-run`.
9. **프로토타입 게이트** — 대시보드 변경분은 `프로토타입` HTML로 먼저 present → 승인 후 `index.html` 병합 (memory `prototype-before-save`).

## 후속 (이번 범위 아님)
- Phase 3: `run_sync` 증분(sync_state)·dedup(URL+docID+title+published_at+broker+ticker)·cache TTL 적용.
- Phase 4: validation 전면 + `source_health.json` 자동 갱신 + Governance 대시.
- Phase 5: consensus revision 추적, broker 비교(median/mean/dispersion), news→event 매핑(raw/normalized/AI 3계층 분리).
- 향후 소스: SEC EDGAR·OpenDART·FRED·Tiingo·OpenBB (adapter 추가만으로, dashboard core 무수정).

---

# Phase A — actual-data-first 전환 (2026-08-27, 방향 변경)

**결정:** 증권사 리포트 / 한경 컨센서스 / 목표주가 / 애널리스트 전망을 **당분간 파이프라인에서 분리**하고, 실제 관측(actual) 데이터 중심 대시보드를 먼저 완성. 한경 자동수집·PDF 파싱·consensus·AI 요약·DB/Redis·대규모 frontend rewrite는 이번 Phase 제외.

## A.1 Metric Audit (Keep / Move / Remove)

| Metric | Current Source | 구분 | 결정 |
|---|---|---|---|
| `price`,`market_cap` | bigdata.com (live) | ACTUAL | **Keep** (NORMALIZED 메인) |
| `volume`,`revenue`,`operating_income`,`net_income`,`eps_actual`,`shares_outstanding`,`book_value`,`cash`,`debt`,`capex`,`inventory` | bigdata tearsheet 재무섹션 **(미검증)** | ACTUAL | **Keep-slot** — 스냅샷 검증 후 구현. 없으면 임의생성 금지 |
| 산업 `asp`,`shipment`,`capacity` | 미연결 | ACTUAL | slot only |
| `daily_change_pct`,`trailing_pe`,`pb`,`ps`,YoY/QoQ,margin,drawdown,momentum,volatility | actual 로부터 계산(미구현) | DERIVED | **Keep-slot** (§5 foundation) |
| `mc_fair_value_mean/p50`,`expected_return`,`p_up`,`fwd_pe` | 대시보드 MC | DERIVED | **Move → derived layer** (⚠ 입력=forward EPS+cycle P/E 제외됨. 리터럴 유지, 후속 재정의) |
| `reverse_dcf_fcff`,`expectations_gap`(삼성) | price+WACC+g 역산 | DERIVED | Move → derived layer |
| `cycle_pe_mid`,`wacc`,`terminal_growth` | 주관 판단 | ASSUMPTION | **Move → assumption layer** (data-source mapping 제외) |
| `target_price`,`tpup` | 증권사 baked-in | REPORT | **Remove → archive** |
| `eps` FY2026/27/28,`eps28dir` | bigdata "consensus" baked | REPORT | **Remove → archive** (analyst forward ≠ `eps_actual`) |
| `optimism_check`(`opt.*` 산문) | 키움·삼성·미래에셋·한화 | REPORT | **Remove → archive** |
| `reg_hbm/dram/china/capex` | 리포트 narrative | REPORT | **Remove → archive** |
| `methods[]` football field | 애널 TP + P/E band + P/B | REPORT+DERIVED | Move → valuation layer (미변경) |
| `valuation_confidence`,`data_coverage` | 메타 점수 | META | Keep |

**의존 화면:** `price`=전 화면 / `eps`·`tp`·`optcheck`=Memory MARKET·COMPANY·VALUATION·PROBABILITY·Home / `mc_*`·`p_up`=Home·Screener·PROBABILITY / `reg_*`=Home. → 리터럴 유지로 전부 비파괴.

## A.2 구현 (파이프라인 측만, index.html 무수정)

| 파일 | 변경 |
|---|---|
| `common/classification.py` | `MetricClass`(ACTUAL/DERIVED/ASSUMPTION/REPORT/META) + `METRIC_CLASS` 맵 + `metric_class()` |
| `common/actual_metrics.py` (신규) | `ACTUAL_METRICS` 레지스트리 + `SOURCE_AVAILABILITY`(price/market_cap=live, 재무=maybe, 산업=none) + `implemented_now()` |
| `common/store.py` | `load_derived`/`load_derived_keys` 추가, `append_derived` dedup, `load_all_normalized(exclude=)` |
| `migrate_inline_to_store.py` | metric class 로 3분할: ACTUAL→`normalized/_migrated.jsonl` · DERIVED→`derived/mc_dashboard.jsonl` · REPORT/ASSUMPTION→`normalized/_report_archive.jsonl`(**보존**). 제외 목록 출력 |
| `build_dashboard_data.py` | `METRIC_TO_LNKEY`=ACTUAL 만 / `DERIVED_METRIC_TO_LNKEY` 신설 / `build_ds()`가 `layer`(NORMALIZED\|DERIVED)·`observation_date`·`retrieved_at` 표기 / `EXCLUDE_STORES=("_report_archive",)` 로 아카이브 소비 제외 |

**검증 결과:** migrate → ACTUAL 4(`price`) · DERIVED 20 · ARCHIVE 21. `build --check` → DS 24노드(price + fv/fv_p50/expret/pup/pe ×4). `_report_archive.jsonl` 21건 보존. index.html/web_deploy 무변경.

## A.3 산출물

`data_sources/prototypes/DS_hook_prototype.html` — **덮어쓰기 완료**. Audit 표 · RAW→NORMALIZED→DERIVED 흐름 · LN() 훅(price=NORMALIZED, fv=DERIVED+formula, tp/eps27/optcheck=REPORT→리터럴 fallback) · provenance 팝업(Layer·Observation Date·Retrieved At·Formula) · 제외 목록 · SRC config.

## A.4 다음 (Phase A 이후)
1. bigdata tearsheet 스냅샷으로 `revenue/operating_income/net_income/eps_actual/shares_outstanding` 제공 여부 검증 → ACTUAL 확장.
2. `price`·재무 history 축적(store append-only) → DERIVED 계산(`revenue_yoy`·`operating_margin`·`trailing_pe`·`drawdown`·`volatility` …).
3. 승인 시 index.html: `LN()` DS 훅 1줄 + `refreshQuotes`/`loadNews` `SRC` 참조.

---

# Phase B — Actual Financial Data Expansion (2026-08-27)

**목표:** 미국(SEC EDGAR) + 한국(OpenDART) **실제 공시 재무**를 연결해 actual-data foundation 확장. 리포트/컨센서스/목표가/forward estimate 계속 제외.

## B.1 신규 파일
| 파일 | 역할 |
|---|---|
| `common/schema.py` | 필드 추가: `source_metric`(원본 XBRL/계정 태그) · `fiscal_year/fiscal_period/original_period/form/filing_date/accession/fs_div/revision_status` · `deprecated_for_actual_dashboard` |
| `common/periods.py` | SEC(`sec_frame`: fy/fp 무시, start/end 날짜로 기간 도출, YTD 제외) · DART(`dart_frame`: reprt_code) 기간 정규화 → `FY2025` / `2026Q2` |
| `common/xbrl_map.py` | metric → 후보 XBRL 태그(SEC) / IFRS 표준계정 id·계정명(DART). 원본 태그 보존 |
| `common/actual_metrics.py` | `ACTUAL_METRICS` 레지스트리 + `SOURCE_AVAILABILITY`(price/market_cap=live, 재무=maybe, 산업=none) |
| `common/derive.py` | **actual 로부터만** 계산: `revenue_yoy/qoq`·`operating/net_income_yoy`·`eps_yoy`·`operating_margin`·`net_margin`·`fcf_margin`·`debt_to_equity`. 각 결과 `is_derived`+`formula`+`input_record_ids`. 음수 기준 YoY 는 flag |
| `sec_edgar/{adapter,parser,schema}.py` | `companyconcept` API(키 불필요, UA 필수). restatement: 같은 period 다중 filing 전부 보존, 최신 `filed`=`latest`. flow=분기(~90d)/연간(~365d), YTD 제외 |
| `opendart/{adapter,parser,schema}.py` | `fnlttSinglAcntAll` (키 필요 → 없으면 `BlockedNoKey`). CFS 우선, OFS fallback + notes |
| `.env.example` | `OPENDART_API_KEY` · `SEC_EDGAR_USER_AGENT` |

## B.2 변경
- `common/classification.py`: `METRIC_CLASS` 에 `eps_basic/eps_diluted/total_assets/total_liabilities/equity/operating_cash_flow`=ACTUAL, `*_yoy/*_qoq/fcf_margin/debt_to_equity`=DERIVED.
- `config/data_sources.json`: `covered[]` 에 `sec_cik`(MU 0000723125·SNDK 0002023554)·`dart_corp_code`(삼성 00126380·SK 00164779, **키 확보 후 검증**). `providers.sec_edgar`·`providers.opendart` 추가. bigdata `role` = price+market_cap 우선, 재무는 SEC/DART 우선. hankyung_* `enabled:false`.
- `build_dashboard_data.py`: `build_actual()` 신설 → `var ACTUAL[slug][GROUP][metric]` (PRICE/INCOME/BALANCE_SHEET/CASH_FLOW/DERIVED) + latest + history[]. `build_ds()` 의 MC 파생 키(`fv/fv_p50/expret/pup/pe`)에 `deprecated_for_actual_dashboard:true`+`legacy`. `_block()` 에 `var ACTUAL` 추가.
- `run_sync.py`: `sec_edgar`·`opendart` 핸들러 + actual 수집 후 `derive.compute()` 자동 실행.

## B.3 실호출 검증 결과 (2026-08-27)
| Company | Provider | Actual Metrics | Periods | Status |
|---|---|---|---|---|
| Micron (MU) | SEC EDGAR | 14 (revenue·op_income·net_income·eps_basic/diluted·cash·debt·total_assets·total_liabilities·equity·inventory·shares·ocf·capex) | FY2009–FY2025 + 분기 (최근 2026Q2) | **Implemented** — 2,444 레코드 |
| SanDisk (SNDK) | SEC EDGAR | 14 (동일) | FY2023–FY2026 + 분기 | **Implemented** — FY2026 rev +175% YoY = spin-off 전후 불연속 가능 → validation WARNING (삭제 안 함, cross-check 권장) |
| 삼성전자 | OpenDART | 구현 완료, 수집 0 | — | **BLOCKED** — `OPENDART_API_KEY` 미설정 |
| SK하이닉스 | OpenDART | 구현 완료, 수집 0 | — | **BLOCKED** — 동상 |

- **derived (actual 기반)**: revenue_yoy 41 · operating_margin 51 · net_margin 51 · eps_yoy 81 · … 총 663건 → `store/derived/actual_metrics.jsonl`. 예) Micron revenue_yoy FY2023 −49.5% / FY2025 +48.9%, operating_margin FY2025 26.1%.
- **validation**: SEC 2,444건 중 WARNING 130 (메모리 사이클 극단 변화 — 삭제 안 함, "검토 필요" 표기).
- **build**: `var DS` 40노드 + `var ACTUAL`(micron/sandisk 각 24 metric) + `var HEALTH`(sec_edgar Healthy). `index.html` 무수정.
- **산출물**: `data_sources/prototypes/DS_hook_prototype.html` 덮어씀 — Audit 표 · Actual Analysis Layer(그룹별 표 + history + provenance 팝업) · Source Health · 완료 보고.

## B.3.1 Finalization Audit (2026-08-28) — 실제 검증 후 수정

이전 Phase B 구현에서 발견·수정한 CRITICAL 결함:

| # | 결함 | 수정 |
|---|---|---|
| C1 | **SEC RAW 파일이 스텁** (`{"metric|tag": "..."}` — 실제 payload 미보존) | `store.save_raw_json()` 신설 → `{_meta:{endpoint,request_urls,cik,http_status,content_hash,user_agent}, data:<실제 concept json>}`. `--rebuild` 로 2,444건 재수집 |
| C2 | normalized→raw 링크 없음 (`raw_ref=null`) | 레코드마다 `raw_ref="raw/sec_edgar/<file>.json#<metric>|<tag>"` |
| C3 | `build_ds` preferred 선택이 **`-len(retrieved_at)` 문자열 길이 정렬** | `store.parse_dt()` datetime 파싱 → `(priority, -filing_date_ts, -retrieved_ts)`. `latest_by_metric` 도 동일. 테스트 `test_latest_filing_selected_by_datetime_not_strlen` |
| C4 | OpenDART 실제 호출 시도 안 함 · health 에 없음 | `run_sync` 에서 키 없으면 `record_health(status="NOT_CONFIGURED", succeeded=False, last_error=...)` |
| C5 | 자동화 테스트 0개 | `data_sources/tests/test_phase_b.py` — 16 테스트(스키마 왕복·dedup 멱등·priority·datetime 선택·validation·provenance 완전성·archive 격리·forward/report core 제외·index.html 무변경) 전부 통과 |
| C6 | health 스키마 빈약 · status 문자열 | `status ∈ {HEALTHY,WARNING,ERROR,NOT_CONFIGURED}` + `last_attempted_sync/last_successful_sync/last_error/records_fetched/records_added/records_updated/duplicates/validation_warnings/response_time_ms` |

IMPORTANT 수정:
- `schema.py` +`available_date`(=filing_date, 스펙 §7 "분기말 아님") · `to_ln_node` 에 `available_date/raw_ref/record_id/validation_status/validation_notes` + `core_eligible:false`.
- `validation.py` +파생 완전성(formula/input_ids 없으면 ERROR) +official provenance 완전성(original_url+accession 없으면 WARNING, raw_ref 없으면 WARNING) +`INSUFFICIENT_DATA` 상태 +ambiguous_account_mapping.
- `store/archive/report.jsonl` 로 이동(기존 `normalized/_report_archive.jsonl`) — 스펙 §12. `append_archive/load_archive`.
- `build_ds` legacy 파생(fv/expret/pup/pe) → `core_eligible:false` + `validation_status:WARNING` + `estimate_dependent_input`.
- 프로토타입 재작성: provenance 팝업이 `record_id → raw_ref → accession → 원문 URL` 체인 + validation + Legacy(core_eligible=false) + 강화된 Health. HTML escape(`esc()`) 적용.

### 검증된 provenance 체인 샘플 (SEC)
```
dashboard: micron / INCOME / revenue = 41,456,000,000 USD  2026Q2  (10-Q)
  → record_id  rec_35aad2614c6f
  → raw_ref    raw/sec_edgar/concepts_micron_2026-08-28.json#revenue|RevenueFromContractWithCustomerExcludingAssessedTax
  → accession  0000723125-26-000015
  → filing/available_date  2026-06-25
  → original_url  https://www.sec.gov/Archives/edgar/data/723125/000072312526000015/0000723125-26-000015-index.htm
  → RAW file exists · _meta.content_hash bce52da9… · _meta.request_url https://data.sec.gov/api/xbrl/companyconcept/CIK0000723125/us-gaap/RevenueFromContractWithCustomerExcludingAssessedTax.json
```

### 판정 이력
- 1차(2026-08-28): **CONDITIONALLY COMPLETE** (SEC COMPLETE / OpenDART BLOCKED — 키 없음).

## B.3.2 OpenDART 실연결 (2026-08-28) → **Phase B COMPLETE**

`data_sources/.env` 에 `OPENDART_API_KEY` 설정(값은 문서·로그 비노출) 후 실호출:

- `python -m data_sources.run_sync --provider opendart` → **삼성·SK하이닉스 336 레코드** 신규(0 실패, 336 VALID), derive 자동 재계산 911건(신규 248 = 삼성 124 + SK 124).
- RAW: `store/raw/opendart/statements_{samsung,skhynix}_2026-08-28.json` (각 ~1.8MB, `_meta.content_hash` + `data.results[14]`).
- 정규화 레코드 필드 검증: `as_of_date`=재무제표 기준일(reprt_code, 예 11012→YYYY-06-30) · `filing_date`=`available_date`=접수일(`rcept_no[:8]`) · `accession`=rcept_no · `fs_div`=CFS(OFS fallback 시 note) · `source_metric`=IFRS 표준계정 id · `original_url`=`dart.fss.or.kr/dsaf001/main.do?rcpNo=` · `raw_ref`=`raw/opendart/<file>#<period>|<metric>|<account_id>`.
- 보안: `opendart.adapter._scrub()` 로 로그·예외·health 에서 키/`crtfc_key=` 값 제거. `store/` 전체 키 스캔 CLEAN. `.env` `.gitignore` 처리.

### 최종: **Phase B COMPLETE**
| provider | records | raw | health | dedup |
|---|--:|---|---|---|
| SEC EDGAR (micron·sandisk) | 2,444 | ✔ real+hash | HEALTHY | 재sync 0 new / 2,444 dup |
| OpenDART (samsung·skhynix) | 336 | ✔ real+hash | HEALTHY | 재sync 0 new (예정) |
| derived actual_metrics | 911 | — | — | micron 578·sandisk 85·samsung 124·skhynix 124 |

- `build_dashboard_data --check`: `var ACTUAL` 4종목 전부 유의미 — micron/sandisk 24 metric, samsung/skhynix 22 metric(INCOME 4·BS 6·CF 2·DERIVED 9·PRICE 1). `var DS` 56 노드.
- 테스트 **18/18 통과** (OpenDART `test_live_opendart_records_exist` / `test_live_opendart_provenance_complete` 포함 — 비어있으면 fail).
- `index.html` / `web_deploy/index.html` 무변경(테스트 강제). 프로토타입 `DS_hook_prototype.html` 갱신(OpenDART live 반영, 백업 후 재생성).

## B.4 BLOCKED / 다음
- **OpenDART**: https://opendart.fss.or.kr 무료 키 → `data_sources/.env` 에 `OPENDART_API_KEY=` → `python -m data_sources.run_sync --provider opendart`. corp_code 는 `--resolve-corp 005930 000660` 로 검증.
- **다음**: price/재무 history 축적 → `trailing_pe`·`pb`·`ps`·`drawdown`·`volatility` / market_cap·volume(bigdata) / 산업 actual(source 미정) / 승인 시 index.html `var ACTUAL` 렌더 + `LN()` DS 훅.
