{
  "name": "AI주식리서치 — data_sources pipeline",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",

  "postCreateCommand": "pip install --upgrade pip && pip install -r data_sources/requirements.txt",

  "containerEnv": {
    "PYTHONUTF8": "1",
    "PYTHONIOENCODING": "utf-8"
  },

  "// secrets": "OPENDART_API_KEY / SEC_EDGAR_USER_AGENT 는 GitHub Repository secret 으로 주입한다 (Settings → Secrets and variables → Codespaces). .env 파일을 만들 필요 없음 — opendart/adapter.py 의 _load_dotenv() 가 os.environ.setdefault 라 이미 있는 환경변수를 덮지 않는다. 키가 없어도 --no-network 재생성은 동작한다.",

  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "files.encoding": "utf8"
      }
    }
  }
}
